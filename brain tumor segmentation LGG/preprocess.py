# find data set here https://www.kaggle.com/datasets/mateuszbuda/lgg-mri-segmentation/code

# collect dataset file locations, load in one by one and pass into one big np.array
# split via skleanr traintestsplit
# pre-process, normalize by mean and std, but train and test seperately 

